# Windows installers

Upload Windows release files here using the exact filename from `website/releases.json`.

Example:

```
ProjectX-0.3-alpha-Setup.exe
```

Download URL generated by the website:

```
downloads/windows/<file>
```
