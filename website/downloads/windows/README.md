# Windows release downloads

**Version:** 0.3.1-alpha.1

Upload Windows release files here using the exact filenames from `website/releases.json`:

```
ProjectX-Setup.exe
SHA256SUMS
```

Download URLs:

```
downloads/windows/ProjectX-Setup.exe
downloads/windows/SHA256SUMS
```
